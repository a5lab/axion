---

- name: deploy app
  hosts: axion_server
  become: yes
  become_user: root
  roles:
    #- role: maintenance
    #- role: sysinfo
    #- role: firewall
    #- role: workstation
    - role: postgresql
    - role: app
    #- role: maintenance
    #  state: absent
